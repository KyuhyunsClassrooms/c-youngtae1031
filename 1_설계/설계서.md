# 1. 프로그램 설계서 (5점)

## 1. 프로젝트 주제
슬라임 토벌 게임

## 2. 핵심 기능 목록
(구현할 기능들을 순서대로 나열하세요.)
1.  턴 시스템 구현
2.  행동 선택 메뉴
3.  피해 계산
4.  회복 시스템
5.  상태이상
6.  난이고 설정
7.  란운드 진행
8.  상태 표시 ui
9.  게임 졸료 및 결과 출력
10. 높은 기록 저장

## 3. 데이터 구조 설계
(프로그램에 필요한 핵심 변수, 배열을 설계하세요. 필요하면 구조체(struct)를 사용할 수도 있습니다.)

**방법 1: 배열 사용 (기본)**
```c
int damageLog[10];
int slimeStats[3];
char actions[4][10];
```

**방법 2: 구조체 사용 (선택)**
```c

typedef struct {
    char name[16];
    int maxHP;
    int hp;
    int def;
    int guard;
    int poisonTurns;
} Unit;
```
